@model IEnumerable<Produto>

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1>Listagem de Produtos</h1>
        <div>
            <a class="btn btn-outline-primary mx-2" asp-controller="Produto" asp-action="Cadastrar">Cadastrar novo Produto</a>
            <a class="btn btn-outline-secondary mx-2" asp-controller="Home" asp-action="Index">Voltar</a>
        </div>
    </div>

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success text-dark" role="alert">
            @TempData["MensagemSucesso"]
        </div>
    }

    @if (TempData["MensagemErro"] != null)
    {
        <div class="alert alert-danger text-light" role="alert">
            @TempData["MensagemErro"]
        </div>
    }

    <table id="Produtos" class="table table-light table-hover tabela-data-asc">
        <thead class="table table-dark">
            <tr>
                <th scope="col">Produto</th>
                <th>Fornecedor</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                @foreach (var Produto in Model)
                {
                    <tr>
                        <td>@Produto.ProdutoNome</td>
                        <td>@Produto.Fornecedor.FornecedorNome</td>
                        <td><a asp-controller="Produto" asp-action="Editar" asp-route-id="@Produto.Id" class="btn btn-warning "><i class="bi bi-pencil-square "></i>Editar</a></td>
                        <td><a asp-controller="Produto" asp-action="Excluir" asp-route-id="@Produto.Id" class="btn btn-danger "><i class="bi bi-trash3-fill "></i>Excluir</a></td>
                    </tr>
                }

            }

        </tbody>
    </table>
</div>